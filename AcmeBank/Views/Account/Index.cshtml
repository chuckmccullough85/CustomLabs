@model AccountSummaryPageModel
@{
}

<nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
    <div class="container-fluid">

        <div class="navbar navbar-collapse collapse d-sm-inline-flex justify-content-between">
            <ul class="navbar-nav flex-grow-1">
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-action="Profile">Profile</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" asp-action="Transfer">Transfer</a>
                </li>
            </ul>
        </div>
    </div>
</nav>



<h1>Account Home Page</h1>
<hr />
Welcome @Model.UserName
<br />

<ul>
    @foreach (var acct in Model.Accounts)
    {
        <li><a asp-action="Detail" asp-route-id="@acct.Id">@acct.Text</a></li>
    }
</ul>

